# Completion after a declared wall limit

2026-09-11, prepared while seed23 is still running. This changes resource
allocation only. It does not permit adaptive additional scientific training.
The user's aggregate compute authorization is unrestricted.

If a frozen four-arm job stops at its wall limit, preserve the full failed
output and receipt. In a fresh job with the same cached image, use the exact
archived Python source, original configuration, data, per-arm seeds and protocol.
Repeat its existing CUDA engineering readiness gate. Reuse completed arms and
completed challenges byte-for-byte. If a completed18,000-update parent has an
incomplete challenge, repeat that fixed challenge in a fresh path. If an arm
has no final resume state, train that arm anew from its declared initialization
for exactly18,000 ordinary updates and its original450 episodes. Preserve the
interrupted trajectory separately; never count it as extra updates to the final
model. This avoids changing a resume contract or silently reusing a weight-only
state without optimizer/RNG history. Any repeated work is separately charged.

The completion bundle copies the prior records for compatibility with existing
review scripts and records the original archive hash, completed/repeated arms,
new source checks and receipts. The prior bundle remains unchanged. Match all
ordinary stream hashes and the early/late episode hashes before declaring the
four-arm experiment complete. The new job has60 H100 minutes and the original
2GiB output ceiling. No method, model, strength, reader or endpoint is selected
according to its evaluation outcome. Keep both original and completion jobs in
the report. Do not rerun numerical/scientific failures under this resource rule.
