# Historical reports

The dated reports and registries are now in the
[document archive](../docs/archive/reports/README.md). Current interpretation and
new results belong in [labnotes](../labnotes.md).

This directory remains as the output location of the legacy
`report_recovery.py` script; regenerating an early registry does not update the
archived original. [Old-to-new path map](../docs/archive/path-map.json).
